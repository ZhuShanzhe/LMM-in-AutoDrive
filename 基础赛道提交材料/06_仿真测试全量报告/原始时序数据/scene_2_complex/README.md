# 场景二日志

| 文件 | 作用 |
|---|---|
| `summary.json` | 8 km 路线、指令、交通流和安全指标汇总 |
| `commands.jsonl` | 指令触发位置、文本、预期动作和执行检查 |
| `events.jsonl` | 运行事件记录 |
| `pipeline_sampled.jsonl` | 从完整闭环日志等间隔抽取的感知、VLA、FSM 与控制记录 |

# 两场景异常与运行观察

两次归档运行均无碰撞、无非法违规并到达终点，没有形成失败案例。仍保留下列运行观察，避免只报告成功终态。

| 记录 ID | 场景 | 现象 | 影响 | 处理与证据 |
|---|---|---|---|---|
| OBS-S1-001 | 场景一 | LaneInvasionSensor 记录 12 次车道线事件 | 经地图与指令复核，非法侵入为 0 | `scene_1_basic/metrics.json` 同时保留原始计数和非法计数 |
| OBS-S2-001 | 场景二 | 相机同步跳帧 1 次 | 未导致计划失败或安全事件 | `scene_2_complex/summary.json` |
| OBS-S2-002 | 场景二 | 当前日志使用 8 条稳定闭环指令，特殊障碍事件关闭 | 不能据此声明完整复杂避障验收 | 报告明确限定结论，完整配置继续保留 |

后续若出现失败运行，应记录运行 ID、随机种子、帧号、原始语音、DrivingIntent、场景实体、决策轨迹、根因、修复提交和复测结果，不得无记录删除。
